'use client';

import React, { use } from 'react'

const ProductCard = () => {
  return (
    <div>
        <button onClick={() => console.log("loged")} >Add</button>
    </div> 
  )
}

export default ProductCard